name: iOS Development Build

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'debug'
        type: choice
        options:
        - debug
        - profile

jobs:
  build-ios-dev:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.32.5'
        channel: 'stable'
    
    - name: Install dependencies
      run: flutter pub get
    
    - name: Build iOS Development
      run: flutter build ios --${{ github.event.inputs.build_type }} --no-codesign
    
    - name: Create Development IPA
      run: |
        cd ios
        mkdir -p build/ios-dev
        cd build/ios-dev
        mkdir -p Payload/Runner.app
        # Copy the built app
        cp -r ../../build/ios/iphoneos/Runner.app Payload/
        # Create IPA
        zip -r Runner-dev.ipa Payload/
    
    - name: Upload Development IPA
      uses: actions/upload-artifact@v3
      with:
        name: healthy-pathway-dev-$(github.run_number)
        path: ios/build/ios-dev/Runner-dev.ipa
        retention-days: 3
    
    - name: Cleanup old dev artifacts
      uses: actions/github-script@v6
      with:
        script: |
          const artifacts = await github.rest.actions.listWorkflowRunArtifacts({
            owner: context.repo.owner,
            repo: context.repo.repo,
            run_id: context.runId,
          });
          // Keep only the latest 1 dev artifact
          const devArtifacts = artifacts.data.artifacts.filter(artifact => 
            artifact.name.startsWith('healthy-pathway-dev-')
          );
          if (devArtifacts.length > 1) {
            const artifactsToDelete = devArtifacts.slice(1);
            for (const artifact of artifactsToDelete) {
              await github.rest.actions.deleteArtifact({
                owner: context.repo.owner,
                repo: context.repo.repo,
                artifact_id: artifact.id,
              });
            }
          } 